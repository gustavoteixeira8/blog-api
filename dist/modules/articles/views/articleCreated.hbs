<style>
  body{
    font-family: Arial, Helvetica, sans-serif;
    max-width: 600px;
    font-size: 18px;
    line-height: 21px;
  }

  table {
    border: 1px solid black;
    border-collapse: collapse;
    border-radius: 5px;
    padding: 5px;
  }

  th {
    border: 1px solid black;
    padding: 5px;
  }

  td {
    border: 1px solid black;
    padding: 5px;
  }
</style>

<div>
  <p>Hello, {{user.username}}</p>

  <p>
    A new article has been created by you, the access link is
    <a href="{{appConfig.url}}/article/{{article.id}}">{{article.title}}</a>.
  </p>

  <p>
    <strong>
      <span style="color: red;">*</span>
      Check the privacy status of the article to make sure it is accessible to everyone
    </strong>
  </p>

  <h3>Article details</h3>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Title</th>
        <th>Slug</th>
        <th>Is public</th>
        <th>Created at</th>
        <th>Author</th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <td>{{article.id}}</td>
        <td>{{article.title}}</td>
        <td>{{article.slug}}</td>
        <td>
          {{#if article.isPublic}}
            Yes
          {{else}}
            No
          {{/if}}
        </td>
        <td>{{convertDate article.createdAt}}</td>
        <td>{{user.username}}</td>
      </tr>
    </tbody>
  </table>

  <p>
    Thank you!<br />
    <strong>{{appConfig.name}}</strong>
  </p>

</div>
